"""Built-in reference skills.

Each module exposes a module-level ``SKILLS`` list that the loader discovers.
These are also the worked examples for the skill SDK: manifest, can_handle,
execute, tests — and no changes to core cognition.
"""
